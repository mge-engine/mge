#include "mge/scene/renderer.hpp"

namespace mge {
    renderer::renderer()
    {}

    renderer::~renderer()
    {}

    void
    renderer::prepare(scene& s)
    {}

    void
    renderer::render(scene& s)
    {}
}