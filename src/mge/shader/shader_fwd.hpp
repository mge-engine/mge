#pragma once
#include "mge/core/types.hpp"

namespace mge {
namespace shader {

    class program;
    class program_element;
    class statement;
    class main_function;
    class expression;

    MGE_DECLARE_REF(program);
    MGE_DECLARE_REF(program_element);
    MGE_DECLARE_REF(statement);
    MGE_DECLARE_REF(expression);
    MGE_DECLARE_REF(main_function);
}
}
