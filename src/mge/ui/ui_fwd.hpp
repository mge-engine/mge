#pragma once
#include "mge/core/types.hpp"
namespace mge {
    namespace ui {
        MGE_DECLARE_REF(context);
        MGE_DECLARE_REF(font);

        MGE_DECLARE_REF(window);
        MGE_DECLARE_REF(menu_bar);
        MGE_DECLARE_REF(menu);
        MGE_DECLARE_REF(main_menu_bar);
        MGE_DECLARE_REF(menu_item);
        MGE_DECLARE_REF(font);
        MGE_DECLARE_REF(text);
        MGE_DECLARE_REF(button);
        MGE_DECLARE_REF(image);

    }
}